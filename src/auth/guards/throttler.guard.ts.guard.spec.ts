import { ThrottlerGuardTsGuard } from './throttler.guard.ts.guard';

describe('ThrottlerGuardTsGuard', () => {
  it('should be defined', () => {
    expect(new ThrottlerGuardTsGuard()).toBeDefined();
  });
});
